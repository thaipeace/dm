import { ListCustomFieldInObjsArrayPipe } from './list-custom-field-in-objs-array.pipe';

describe('ListCustomFieldInObjsArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ListCustomFieldInObjsArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
