import { ListBySpecificFieldsPipe } from './list-by-specific-fields.pipe';

describe('ListBySpecificFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListBySpecificFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
