import { MacValidatorDirective } from './mac-validator.directive';

describe('MacValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MacValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
